## 1.0.9（2025-12-25）
解决bl-tree/index.vue 和 bl-tree/node.vue 使用了 <bl-tree-node>，Vite 可能通过 easycom 解析导致路径错误
## 1.0.8（2025-12-25）
bl-share-poster 未在 index.ts 中导出，且 components.d.ts 中也没有类型声明。虽然可通过 easycom 解析，但 Vite 在静态分析时可能查找 .vue 而非 .uvue。检查是否需要添加导出或类型声明
## 1.0.7（2025-12-25）
删除了不支持的icons
## 1.0.6（2025-12-17）
暴露index.d.ts类型文件到插件目录
# 更新日志

## 1.0.4（2025-12-17）

### 🎉 重大改进

- **类型系统重构**
  - ⚠️ **破坏性变更**：删除所有组件的 `type.uts` 文件（107 个），类型定义已全部迁移到 `index.d.ts`
  - ⚠️ **破坏性变更**：删除 105 个仅用于类型导出的 `index.uts` 文件
  - 类型定义从分散在各组件目录集中到单一的 `uni_modules/belay-unix/index.d.ts` 文件

### ✨ 新增功能

- **细化的类型别名**
  - 新增 60+ 个精细化的类型别名，如 `BlButtonType`、`BlButtonSize`、`BlInputType`、`BlTagEffect` 等
  - 所有组件的 Props 定义现在使用类型别名而非联合类型字面量
  - 支持更好的类型推断和代码提示
  
- **增强的代码提示**
  - 输入 `siz` 时自动提示 `size` 属性
  - 输入 `size="` 时自动提示所有可用值（如 `'large' | 'medium' | 'default' | 'small' | 'mini'`）
  - 完整支持 Props、Events、Slots、Instance 方法的类型提示
  - 在 script 中导入类型：`import type { BlButtonProps, BlInputType } from '@/uni_modules/belay-unix'`

### 🔧 改进

- 简化项目结构，类型定义统一管理
- 优化 `components/index.uts`，仅保留组件导出，移除所有类型导出语句
- 更新 `components.d.ts`，确保全局组件类型声明正确
- 减少 212 个文件（107 个 type.uts + 105 个 index.uts）
- 提升 IDE 性能和类型检查速度

### 📖 文档

- 更新 `components/index.uts` 注释，说明类型定义迁移至 `index.d.ts`
- 添加类型系统使用示例和最佳实践

### 迁移指南

如果你之前直接从组件目录导入类型（不推荐），需要更新导入路径：

```typescript
// ❌ 旧方式（不再支持）
import type { BlButtonProps } from '@/uni_modules/belay-unix/components/bl-button/type.uts'

// ✅ 新方式
import type { BlButtonProps } from '@/uni_modules/belay-unix'
```

如果你使用 Easycom 自动导入组件（推荐方式），无需任何改动。

## 1.0.3（2025-12-17）
### ✨ 新增功能

- **TypeScript 类型定义支持**
  - 新增 `components/index.d.ts` 文件，提供 TypeScript 代码提示
  - 支持通过 `import type { BlButtonProps } from '@/uni_modules/belay-unix/components'` 导入类型
  - 更新 `package.json` 的 `exports` 配置，添加 `"./components"` 类型导出路径

### 🔧 改进

- 优化类型导出配置，提升开发体验
- 完善 TypeScript 项目中的代码提示支持
## 1.0.2（2025-12-17）

### 🐛 修复

- 修复 `package.json` 中 `files` 字段缺少 `locales` 目录的问题
- 确保发布 npm 包时，`locales` 目录被正确包含

## 1.0.1（2025-12-17）

### 🗑️ 清理

- 删除部分无用文件

## 1.0.0（2025-12-17）

🎉 **首次发布**

Belay-Unix 组件库的第一个正式版本，提供 108 个高质量组件，支持 uni-app x 和 uni-app 项目。

### ✨ 新增功能

#### 组件库核心

- **108 个组件**：提供完整的组件生态，覆盖基础、表单、反馈、展示、导航等场景
  - **基础组件（17 个）**：按钮、输入框、文本、图标、图片、头像、徽标、分割线、金额、评分、分段控制器、单元格、视图容器、间距、细线、空状态、错误捕获
  - **表单组件（19 个）**：表单、输入框、数字输入框、文本域、自动完成、单选框、单选框组、单选框弹窗、复选框、复选框组、复选框弹窗、选择器、选择器弹窗、搜索框、滑块、开关、树形选择器、上传器
  - **反馈组件（15 个）**：对话框、抽屉、模态框、弹出层、气泡确认框、气泡卡片、提示框、通知、消息提示、加载中、进度条、旋转器、通告栏、漫游式引导、工具提示
  - **展示组件（25 个）**：标签、可选中标签、排序标签、徽标、头像、卡片布局、折叠面板、倒计时、描述列表、浮动按钮、二维码、结果页、骨架屏、统计数值、表格、时间轴、穿梭框、树形控件、水印、日历、轮播图、步骤条、步骤、空状态、结果
  - **导航组件（11 个）**：标签页、标签按钮、标签面板、标签栏、分页、菜单、返回顶部、自定义导航栏、小程序自定义 TabBar、底部栏、筛选器
  - **布局组件（9 个）**：行、列、栅格、栅格项、页面容器、页面样式、滚动视图、列表视图、间距容器
  - **其他组件（12 个）**：视频、分享应用消息、分享对话框、分享海报、海报绘制器、预览上下文、主题、主题提供者、主题根、国际化提供者、传送门、无操作组件

#### 工具类库

- **BlI18n**：国际化工具类
  - 支持多语言切换
  - 内置中文（zh-CN）和英文（en-US）语言包
  - 支持自定义语言包
  - 支持从本地文件加载语言资源
  - 支持本地存储语言设置

- **BlTheme**：主题系统
  - 支持运行时主题切换
  - 支持主题配置合并
  - 支持本地存储主题设置
  - 支持 CSS 变量（H5 平台）
  - 提供主题提供者组件

- **BlEventBus**：事件总线
  - 支持组件间通信
  - 支持事件订阅和发布
  - 支持事件命名空间

- **BlPageContext**：页面上下文
  - 提供页面生命周期 hooks
  - 支持页面状态管理
  - 支持路由信息获取

#### 图标库

- **118 个图标**：提供丰富的图标资源
  - 支持按需导入
  - 提供 TypeScript 类型定义
  - 覆盖常用业务场景

#### 样式系统

- **SCSS 变量系统**：统一的样式变量管理
  - 颜色系统（主色、文本色、背景色、边框色等）
  - 间距系统
  - 字体系统
  - 圆角系统
  - z-index 层级系统

#### 语言包

- **内置语言包**：中文（zh-CN）和英文（en-US）
- **类型定义**：完整的 Locale 类型定义
- **统一导出**：通过 `locales/index.uts` 统一导出

### 🎯 核心特性

- **跨平台支持**：支持 uni-app x 和 uni-app（Vue2/Vue3）项目
- **双文件格式**：同时提供 `.uvue`（uni-app x）和 `.vue`（uni-app）文件
- **类型安全**：完整的 UTS 类型定义，提供类型提示
- **按需导入**：支持按需导入组件，减少打包体积
- **全局注册**：支持通过 `app.use()` 全局注册所有组件
- **Easycom 支持**：支持 easycom 自动导入配置
- **主题定制**：支持主题系统，可自定义主题配置
- **国际化**：内置国际化支持，可扩展多语言
- **文档完善**：每个组件都提供详细的 README 文档

### 📦 安装方式

- 通过 uni-app 插件市场安装
- 通过 npm/pnpm/yarn 安装
- 通过 Git 克隆安装

### 📚 文档

- 组件使用文档
- 主题系统使用指南
- 国际化使用指南
- Easycom 配置指南
- 组件导入说明
