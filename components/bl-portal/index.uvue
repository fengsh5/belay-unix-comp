<template>
	<view></view>
</template>

<script lang="uts">
	export default {
		name: 'BlPortal',
		props: {
			/**
			 * 目标容器选择器
			 */
			target: {
				type: String,
				default: 'body'
			}
		},
		mounted() {
			// Portal 组件用于将内容渲染到指定的 DOM 节点
			// 在 uni-app x 中，可以通过条件渲染实现类似效果
			// 这里提供一个基础实现，实际使用时可能需要结合具体的业务场景
		}
	}
</script>

<style lang="scss">
	/* 引入组件样式 */
	@import './index.scss';
</style>
